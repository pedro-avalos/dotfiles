"""Provides an access point for my useful scripts."""

from pathlib import Path
from . import paths

select_shot_clip: Path = paths.scripts_dir / "select_shot_clip.sh"
select_shot: Path = paths.scripts_dir / "select_shot.sh"
full_shot: Path = paths.scripts_dir / "full_shot.sh"
