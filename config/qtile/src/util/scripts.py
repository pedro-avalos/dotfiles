"""Provides an access point for my useful scripts."""

from . import paths

select_shot_clip = paths.scripts_dir / "select_shot_clip.sh"
select_shot = paths.scripts_dir / "select_shot.sh"
full_shot = paths.scripts_dir / "full_shot.sh"
